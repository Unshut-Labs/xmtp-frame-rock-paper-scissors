// export const play = async (
//   conversationId: string,
//   playerAddress: string,
//   move: "ROCK" | "PAPER" | "SCISSORS"
// ) => {
//     let currentGameId = await getGameId(conversationId);
//     if (!currentGameId) {
//         currentGameId = await createGame(conversationId);
//     }
// };

// const getGameId = (conversationId: string) => {
    
// }